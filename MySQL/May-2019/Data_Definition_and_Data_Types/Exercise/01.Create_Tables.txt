CREATE TABLE `minions`(
	id INT NOT NULL AUTO_INCREMENT,
    name VARCHAR(45) NOT NULL,
    age INT,
    PRIMARY KEY (id)
);

CREATE TABLE `towns`(
	id INT NOT NULL AUTO_INCREMENT,
    name VARCHAR(45) NOT NULL,
    PRIMARY KEY (id)
);